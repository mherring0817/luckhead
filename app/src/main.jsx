import React from "react";
import { createRoot } from "react-dom/client";
import Luckhead from "./Luckhead.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Luckhead />
  </React.StrictMode>
);
